package pl.plantoplate.REST.mail;

public enum EmailType {
    /**
     * Type of email to send during registration
     */
    registration,

    /**
     * Type of email to send when user reset password
     */
    reset
}
